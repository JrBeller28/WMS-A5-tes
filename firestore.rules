rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Prototype environments: allow all read/write
      allow read, write: if true;
    }
  }
}
